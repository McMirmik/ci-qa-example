{
  "name": "selenium-typescript",
  "version": "1.0.0",
  "description": "Automation Testing with Selenium and Typescript",
  "main": "index.js",
  "scripts": {
    "typescript": "./node_modules/.bin/tsc --p tsconfig.json",
    "test": "./node_modules/.bin/mocha --recursive --timeout 10000 dist/**/*.test.js",
    "refresh": "npm run typescript && npm run test"
  },
  "license": "MIT",
  "dependencies": {
    "@types/mocha": "^5.2.7",
    "@types/selenium-webdriver": "^4.0.3",
    "chromedriver": "latest",
    "mocha": "^5.2.0",
    "newman": "^4.5.7",
    "selenium-webdriver": "latest",
    "typescript": "^3.6.4"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
